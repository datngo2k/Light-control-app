import 'package:flutter/cupertino.dart';

class RoomScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Register room request")
    );
  }
}